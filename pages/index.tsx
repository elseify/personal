import type { NextPage } from 'next';

import styles from './index.module.scss';

const Home: NextPage = () => {
  return (
    <div className={styles.block}>
      {/* pass */}
    </div>
  );
}

export default Home;
